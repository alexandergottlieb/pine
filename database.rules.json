{
  "rules": {
    "user": {
      "$user": {
        ".read": "$user == auth.uid",
        ".write": "$user == auth.uid",
        "words": {
          "$treebank": {
            ".indexOn": [
              "index"
            ]
          }
        }
      }
    }
  }
}
